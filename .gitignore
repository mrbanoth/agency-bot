# Never push secrets to GitHub
.env
__pycache__/
*.pyc
venv/

# Tracking files — committed by the bot after each run (do NOT ignore these)
# leads.csv, sent_log.csv, conversations.json, replied_log.csv → tracked in repo

# Ignore other random JSON (audit files etc) but not conversations.json
audit_*.json
